import React from "react";
import PlaceholderScreen from "./PlaceholderScreen";

export default function CalendarScreen() {
  return (
    <PlaceholderScreen
      emoji="🗓️"
      title="Calendar"
      body="Appointments, reminders, and the daily overview are ported from the PWA next."
    />
  );
}
