import React from "react";
import PlaceholderScreen from "./PlaceholderScreen";

export default function GalleryScreen() {
  return (
    <PlaceholderScreen
      emoji="📸"
      title="Gallery"
      body="Romere's photo album, backed by expo-image-picker for the native photo/camera flow, lands here next."
    />
  );
}
